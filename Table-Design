Table Design:-

  CREATE TABLE "INTERNAL_MEETINGS" 
   (	"MEETING_ID" NUMBER , 
	"TITLE_DOMAIN" VARCHAR2(200) NOT NULL ENABLE, 
	"TITLE_NAME" VARCHAR2(500) NOT NULL ENABLE, 
	"PRESENTER_ID" VARCHAR2(100) NOT NULL ENABLE, 
	"PRESENTER_NAME" VARCHAR2(200) NOT NULL ENABLE, 
	"PRESENTER_DEPT_NO" NUMBER NOT NULL ENABLE, 
	"MEETING_DATE" DATE NOT NULL ENABLE, 
	"MEETING_TIME" VARCHAR2(20) NOT NULL ENABLE, 
	"APPROVED_STS" VARCHAR2(1) DEFAULT 'N' NOT NULL ENABLE, 
	"AUDIENCE" VARCHAR2(4000), 
	 PRIMARY KEY ("MEETING_ID")
  USING INDEX  ENABLE
   ) ;
